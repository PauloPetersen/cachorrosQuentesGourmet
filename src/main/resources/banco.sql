CREATE TABLE ingrediente
(
    nome_ingrediente VARCHAR(255) NOT NULL,
    valor_ingrediente VARCHAR(255) NOT NULL,
    PRIMARY KEY (id_pessoa)
);

INSERT INTO pessoa (nome_pessoa, email_pessoa) VALUES ('Paulo', 'teste@teste.com.br');
